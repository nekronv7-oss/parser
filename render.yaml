services:
  - type: web_service
    name: gift-parser-bot
    env: python
    plan: starter
    branch: master
    buildCommand: pip install -r requirements.txt
    startCommand: python marketpars.py
